
export type Plans = {
    _id: string;
    plan: string;
    price: number;
    features: string[]; // Features alanı string array olarak güncellendi.
    createdAt: string;
    updatedAt: string;
    __v: number;
};

